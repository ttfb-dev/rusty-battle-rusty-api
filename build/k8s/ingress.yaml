apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: K8S_PROJECT_NAME
  namespace: K8S_NAMESPACE
spec:
  rules:
    - host: avatar.k8s.robots-game.ru
      http:
        paths:
          - path: /
            backend:
              serviceName: K8S_PROJECT_NAME-nginx
              servicePort: 80
